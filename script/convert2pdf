#/bin/sh

# Convert jpeg/png/gif/drawio file to the PDF file
# The error messages are redirected to /dev/null
ls image_src/*.jpg image_src/*.png image_src/*.gif image_src/*.drawio image_src/*.pdf 2>/dev/null | parallel image_src/convert_image   {} image/{/.}.pdf
